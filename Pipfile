[[source]]
name = "pypi"
url = "https://pypi.org/simple"
verify_ssl = true

[dev-packages]

[packages]
requests = ">=2.21.0"
slackclient = ">=1.3.0"
slackeventsapi = ">=2.1.0"
websocket-client = ">=0.54.0"
polling = ">=0.3.0"
redis = ">=3.0.1"
Flask = ">=1.0.2"
Jinja2 = ">=2.10"
hvac = ">=0.7.2"
gunicorn = ">=19.9.0"
pyyaml = ">=3.13"

[requires]
python_version = "3.7"
